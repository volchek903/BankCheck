services:
  bankcheck-bot:
    build: .
    env_file:
      - path: .env
        required: false
    environment:
      STATE_FILE: /data/state.json
    volumes:
      - bankcheck-data:/data
    restart: unless-stopped

volumes:
  bankcheck-data:
